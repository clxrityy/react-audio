import { NotFoundPage } from "nextra-theme-docs";

export default function NotFound() {
  return (
    <NotFoundPage content="Submit an issue on GitHub if you think this is a bug.">
      <h1>Page Not Found</h1>
    </NotFoundPage>
  );
}
