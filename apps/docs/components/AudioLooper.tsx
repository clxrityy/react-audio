"use client";

import {
  AudioLooper as ReactAudioLooper,
  type AudioLooperProps
} from "@clxrity/react-audio";

export function AudioLooper(props: AudioLooperProps) {
  return <ReactAudioLooper {...props} />;
}
